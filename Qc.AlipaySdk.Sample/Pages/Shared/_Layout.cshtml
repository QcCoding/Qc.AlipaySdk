<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] 支付宝开放平台 Sdk示例</title>

    @RenderSection("Styles", required: false)
</head>
<body>
    <div class="container">
        <fieldset>
            <legend>支付宝开放平台 Sdk接口列表</legend>
            <ul>
              
            </ul>
        </fieldset>
        <div>
            <fieldset>
                <legend>@ViewData["Title"]</legend>
                @RenderBody()
            </fieldset>
        </div>
    </div>

    @RenderSection("Scripts", required: false)
</body>
</html>
